{
  "file": "queries/my_query1.sql",
  "reviewed_at": "2026-05-25T18:42:08.882061",
  "issues_found": 3,
  "review": {
    "issues": [
      {
        "severity": "Critical",
        "category": "Correctness",
        "title": "Incorrect table references",
        "line": "1",
        "problem": "The query references tables 'users' and 'orders' which do not exist in the provided schema. The correct tables are'sigma_analytics.dim_customer' and'sigma_analytics.fact_transactions'.",
        "fix": "SELECT c.customer_name, c.email, t.transaction_id\nFROM sigma_analytics.dim_customer c, sigma_analytics.fact_transactions t\nWHERE c.customer_id = t.customer_id\nAND t.amount > (\n    SELECT AVG(amount)\n    FROM sigma_analytics.fact_transactions\n    WHERE customer_id = c.customer_id\n);"
      },
      {
        "severity": "High",
        "category": "Performance",
        "title": "Correlated subquery",
        "line": "4",
        "problem": "The correlated subquery can be inefficient as it runs for each row in the outer query. This can be optimized.",
        "fix": "SELECT c.customer_name, c.email, t.transaction_id\nFROM sigma_analytics.dim_customer c\nJOIN (\n    SELECT customer_id, AVG(amount) as avg_amount\n    FROM sigma_analytics.fact_transactions\n    GROUP BY customer_id\n) avg_amt\nON c.customer_id = avg_amt.customer_id\nJOIN sigma_analytics.fact_transactions t\nON c.customer_id = t.customer_id\nWHERE t.amount > avg_amt.avg_amount;"
      },
      {
        "severity": "Medium",
        "category": "Readability",
        "title": "Lack of table aliases",
        "line": "1-4",
        "problem": "The query lacks meaningful table aliases which makes it harder to read and understand.",
        "fix": "SELECT c.customer_name, c.email, t.transaction_id\nFROM sigma_analytics.dim_customer c\nJOIN (\n    SELECT customer_id, AVG(amount) as avg_amount\n    FROM sigma_analytics.fact_transactions\n    GROUP BY customer_id\n) avg_amt\nON c.customer_id = avg_amt.customer_id\nJOIN sigma_analytics.fact_transactions t\nON c.customer_id = t.customer_id\nWHERE t.amount > avg_amt.avg_amount;"
      }
    ],
    "corrected_sql": "SELECT c.customer_name, c.email, t.transaction_id\nFROM sigma_analytics.dim_customer c\nJOIN (\n    SELECT customer_id, AVG(amount) as avg_amount\n    FROM sigma_analytics.fact_transactions\n    GROUP BY customer_id\n) avg_amt\nON c.customer_id = avg_amt.customer_id\nJOIN sigma_analytics.fact_transactions t\nON c.customer_id = t.customer_id\nWHERE t.amount > avg_amt.avg_amount;",
    "summary": "The query had several correctness issues due to incorrect table references and lacked performance optimizations and readability improvements."
  }
}